# human-contact-invasive-species-model
ArcGIS ModelBuilder tool (with Python export) to buffer human contact locations, overlay invasive species polygons, and summarize overlap areas in chosen units.
